name: Shell commands

on: [push]

jobs:
    run-shell-command:
        runs-on: ubuntu-latest
        steps:
            - name: echo a string
              continue-on-error: false
              run: echo "Alô mundo"
            - name: multiline script
              run: |
                  node -v
                  npm -v
